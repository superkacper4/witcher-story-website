import React from "react";
import styles from "./DandelionIcon.module.scss"

const DandelionIconText = ({IconText}) => (
    <div className={styles.text}>
        {IconText}
    </div>
)

export default DandelionIconText;